{
  // Protected oh-my-opencode plugin config -- .jsonc loads first, .json is ignored.
  // Only override what the plugin's built-in fallback chains can't auto-detect.
  // Everything else uses defaults: Opus for planning, GPT for reasoning, Gemini for visual.
  "$schema": "https://raw.githubusercontent.com/code-yeongyu/oh-my-opencode/master/assets/oh-my-opencode.schema.json",
  "google_auth": false
}
